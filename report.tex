\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{xurl}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,calc,backgrounds}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue
}

\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
\graphicspath{{outputs/report_figures/}}

\title{Uncertainty-Aware Persistent 3D World Models: \\
Student-Scale Framework Replication and Extension\\
\large CS 599 G1 (Spring 2026) --- Deep Visual Generative Models --- Final Paper}
\author{Jitarth \\
Boston University \\
\texttt{jitarth@bu.edu} \\
BU ID: U05789425}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Long-horizon \emph{visual} prediction---a core theme in deep generative modeling for video---requires
a model to remember previously observed 3D scene content across time.
The course reference manuscript \emph{Learning 3D Persistent Embodied World Models} proposes persistent
3D memory together with a diffusion-based generative world model to forecast RGB-D observations from an
embodied agent's experience~\cite{persistent3dworld}.
Faithfully reproducing that full diffusion-based architecture and training regime was outside the compute
budget of this semester-long project.

We instead replicate the \emph{core persistent-memory framework} at student scale:
from context RGB-D frames and camera parameters we lift pixels into 3D, accumulate a dense voxel memory,
render the memory into each target viewpoint, condition future-frame prediction on those renders, roll out
multi-step predictions, and update memory over the rollout.
To keep experiments tractable while isolating memory and uncertainty effects, we use a
\emph{ConvGRU} spatiotemporal backbone rather than a full diffusion/DiT generator.

Our extension estimates per-pixel uncertainty (log-variance), maps it to confidence, and applies
confidence-gated memory writes so that unreliable generated pixels are less likely to corrupt the
persistent buffer.
On a $200$-clip MOVi-A subset ($320$ validation windows; uniform evaluation harness), the
memory-conditioned ConvGRU improves over a no-memory ConvGRU on validation L1
($0.0214\!\rightarrow\!0.0188$; $-12.1\%$),
dynamic-region L1 ($0.1023\!\rightarrow\!0.0890$; $-13.0\%$),
and PSNR ($25.57\!\rightarrow\!26.68$~dB).
Adding uncertainty-gated writes further reduces memory-covered L1
($0.0577\!\rightarrow\!0.0443$; $-23.2\%$),
with uncertainty positively correlated to per-pixel error ($r{=}0.150$).
These results support uncertainty-aware memory updates as a practical way to improve the \emph{reliability}
of persistent geometric memory in student-scale world models.

\medskip
\noindent\textbf{Course alignment.}
This project studies future visual prediction with explicit 3D memory, connects to diffusion-based
generative world modeling through the reference manuscript, and analyses behaviour quantitatively and
qualitatively---while being honest that the primary experiments use a compute-feasible recurrent backbone.
\end{abstract}

\section{Introduction}

Predicting future RGB views of a 3D scene is a central problem in \emph{generative} video modeling:
the model must hallucinate plausible temporal evolution while respecting geometry, motion, and
visibility.
When the camera or objects move, purely frame-local predictors forget distant scene content; recurrent
predictors helped by context still struggle as errors compound over time.
The reference manuscript on persistent 3D embodied world models~\cite{persistent3dworld} argues for an
\emph{explicit, geometrically grounded} memory into which observations are written and from which novel
views can be queried, paired with a diffusion-style generative predictor.

\paragraph{Scope and objective.}
Our objective is \textbf{not} exact replication of every component of that architecture at original scale,
but \textbf{mechanism replication}: we isolate the \emph{memory write, read/render, memory-conditioned
prediction, and memory update} loop that defines the framework, and we test a focused extension on top.

Although the original paper uses a diffusion-based generator, reproducing that component was outside the
compute budget of this course project. We therefore use ConvGRU as a compute-feasible backbone to
isolate and evaluate the persistent-memory mechanism and our uncertainty-gated memory update extension.
This substitution changes the generator, but preserves the memory-write, memory-read, and memory-update
loop that defines the original framework.
The substitution of ConvGRU for the original diffusion backbone is a compute-driven simplification, \emph{not}
a claim that ConvGRU is a better generative model.

\paragraph{Research question.}
We ask:
\begin{quote}
\emph{Does persistent 3D memory improve future-frame prediction at student scale, and can
uncertainty-gated memory updates improve the reliability of that memory?}
\end{quote}

\paragraph{Contributions.}
\begin{enumerate}[leftmargin=1.5em,itemsep=2pt]
  \item \textbf{Framework replication.} A student-scale implementation of persistent 3D voxel memory with
        target-view rendering, memory-conditioned rollout, and updates---aligned with the reference
        manuscript's loop but without claiming full architecture match.
  \item \textbf{Controlled ConvGRU study.} Three matched variants: no-memory, memory-conditioned, and
        memory-conditioned with uncertainty-gated writes, under identical data splits and comparable training
        budgets.
  \item \textbf{Uncertainty-gated memory updates.} Heteroscedastic uncertainty with confidence thresholding
        to limit unreliable generated content from being written into memory.
  \item \textbf{Evaluation harness.} Unified validation metrics plus diagnostic slices (high motion,
        occlusion recovery, high memory coverage, depth-edge-heavy).
\end{enumerate}

\section{Related Work}

\subsection{Persistent 3D world models}
The course reference manuscript~\cite{persistent3dworld} frames embodied world modeling as maintaining a
persistent 3D scene representation: RGB-D observations are fused into memory using known intrinsics and
extrinsics; the model predicts future RGB-D conditioned on pose and on memory readouts; memory evolves as
the agent acts and predicts. Our work preserves this \emph{interaction pattern} at reduced scale.

\subsection{Video prediction and recurrent visual models}
Convolutional recurrent cells (ConvLSTM~\cite{convlstm}, ConvGRU~\cite{convgru}) are standard for
deterministic or lightly stochastic video prediction. ConvGRU offers a lightweight, single-pass
spatiotemporal backbone suitable for controlled experiments when diffusion-scale training is infeasible.

\subsection{Diffusion video models}
Conditional video diffusion is the dominant modern family for high-fidelity generative
video~\cite{videodiffusion} and matches the \emph{spirit} of the large generative backbone in the reference
manuscript. We did not treat full diffusion/DiT reproduction as the main project deliverable because of
compute and engineering cost; we position a diffusion-style branch as \textbf{future work}, not as a failed
baseline.

\subsection{Uncertainty-aware prediction}
Heteroscedastic uncertainty and per-pixel variance modeling~\cite{kendallgal2017} let a network signal
where predictions are unreliable. We reuse this machinery both as an auxiliary loss and as a confidence map
for selectively writing predicted views into memory.

\subsection{MOVi / Kubric}
MOVi-A (Kubric~\cite{kubric}) provides RGB-D video, camera intrinsics, and poses in synthetic scenes with
clean geometry---useful for testing unprojection, voxel fusion, and render/read correctness before moving
to noisy real-world embodied data.

\section{Replication Scope: Framework, Not Full Architecture}
\label{sec:replication-scope}

Table~\ref{tab:scope} summarises what we replicate versus what we simplify. The intent is to prevent
misreading this report as claiming an exact, full-scale reproduction of the reference manuscript.

\begin{table}[!htb]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.28\textwidth}p{0.38\textwidth}p{0.28\textwidth}}
\toprule
Original paper component & Our implementation & Status \\
\midrule
Persistent 3D memory &
Dense voxel memory from RGB-D and poses &
Replicated at student scale \\
RGB-D observations &
MOVi-A RGB-D clips &
Replicated \\
Camera pose / action conditioning &
Camera and relative target-pose conditioning &
Partially replicated \\
2D-to-3D lifting &
Unprojection using intrinsics, pose, and depth &
Replicated \\
Memory read / render &
Target-view memory render and coverage mask &
Replicated \\
Memory-conditioned prediction &
ConvGRU conditioned on rendered memory &
Simplified but replicated \\
Diffusion / DiT generator &
Not used as primary model (compute budget) &
Not fully replicated \\
DINO / semantic feature memory &
Not used (RGB-focused memory) &
Simplified \\
Large embodied training &
MOVi-A subset (200 clips) &
Simplified \\
Uncertainty-gated writes &
Confidence-thresholded updates for predicted frames &
Added (our extension) \\
\bottomrule
\end{tabular}%
}
\caption{What we replicate versus what we simplify. We target \emph{mechanism replication} of the
persistent-memory loop, not bit-for-bit match to the reference implementation.}
\label{tab:scope}
\end{table}

\section{Task Setup and Notation}
\label{sec:task}

Let a clip provide RGB frames $\{I_t\}_{t=1}^{T}$, depth maps $\{D_t\}_{t=1}^{T}$, camera poses
$\{P_t\}_{t=1}^{T}$ (each a \textbf{camera-to-world} transform, as in Kubric / our loader), and intrinsics $K$. We split each clip into windows of length $T_c + T_p$: the first
$T_c$ frames are \emph{context} and the next $T_p$ frames are \emph{targets} to predict.

The model receives context RGB $\{I_t\}_{t=1}^{T_c}$, context poses $\{P_t\}_{t=1}^{T_c}$, and target poses
$\{P_t\}_{t=T_c+1}^{T_c+T_p}$, and outputs predicted RGB $\{\hat I_t\}_{t=T_c+1}^{T_c+T_p}$. Memory
variants additionally consume a rendered memory tensor per target frame (Section~\ref{sec:method-render}).

MOVi clips are exported at $128\times128$; we train and evaluate at $64\times64$. All final runs use
$T_c=T_p=4$.

\section{Method}
\label{sec:method}

\subsection{Overall framework}

At a high level, each rollout executes:
\emph{build memory from context} $\rightarrow$ \emph{render memory at target poses} $\rightarrow$
\emph{predict future RGB (and auxiliary depth / uncertainty as enabled)} $\rightarrow$
\emph{update memory} along the predicted trajectory.
Figure~\ref{fig:architecture} shows the ConvGRU-based instantiation; Table~\ref{tab:backbone-compare}
clarifies how this differs from the diffusion-based generator in the reference manuscript.

\begin{figure}[!htb]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=10mm and 14mm,
  every node/.style={font=\small},
  blk/.style={rectangle, draw, rounded corners=2pt,
              minimum width=24mm, minimum height=8mm,
              align=center, fill=blue!5},
  memblk/.style={rectangle, draw, rounded corners=2pt,
                 minimum width=24mm, minimum height=8mm,
                 align=center, fill=orange!10},
  outblk/.style={rectangle, draw, rounded corners=2pt,
                 minimum width=24mm, minimum height=8mm,
                 align=center, fill=green!10},
  inblk/.style={rectangle, draw, rounded corners=2pt,
                minimum width=24mm, minimum height=8mm,
                align=center, fill=gray!10},
  flowarrow/.style={-{Latex[length=2mm]}, thick},
  gatearrow/.style={-{Latex[length=2mm]}, thick, dashed, red!70!black}
]
\node[inblk]                                (ctx)    {Context $\{I_t,P_t,D_t\}_{t=1}^{T_c}$};
\node[blk,    right=of ctx]                 (enc)    {Image\\Encoder $E$};
\node[blk,    right=of enc]                 (gru)    {ConvGRU\\cell};
\node[blk,    right=of gru]                 (fuse)   {Fuse\\$1\!\times\!1$ conv};
\node[blk,    right=of fuse]                (gru2)   {ConvGRU\\update};
\node[outblk, right=of gru2]                (drgb)   {RGB\\Decoder $D_{\text{rgb}}$};
\node[outblk, above=6mm of drgb]            (ddepth) {Depth\\Decoder $D_d$};
\node[outblk, below=6mm of drgb]            (dunc)   {Uncertainty\\Head $U$};
\node[memblk, below=18mm of ctx]            (vox)    {Build 3D\\Voxel Memory $\mathcal{M}$};
\node[memblk, below=18mm of gru]            (rend)   {Render at\\target pose $P_t$};
\node[memblk, below=18mm of fuse]           (menc)   {Memory\\Encoder $E_m$};
\node[blk,    below=18mm of gru2]           (pose)   {Pose\\Features $\phi$};

\draw[flowarrow] (ctx)   -- (enc);
\draw[flowarrow] (enc)   -- (gru);
\draw[flowarrow] (gru)   -- node[above]{$h_{T_c}$} (fuse);
\draw[flowarrow] (fuse)  -- (gru2);
\draw[flowarrow] (gru2)  -- (drgb);
\draw[flowarrow] (gru2)  |- (ddepth);
\draw[flowarrow] (gru2)  |- (dunc);

\draw[flowarrow] (ctx)   -- (vox);
\draw[flowarrow] (vox)   -- (rend);
\draw[flowarrow] (rend)  -- (menc);
\draw[flowarrow] (menc)  -- (fuse);

\draw[flowarrow] (pose)  -- (fuse);

\node[right=2mm of drgb,align=left,anchor=west]   (rgbout) {$\hat I_t$};
\node[right=2mm of ddepth,align=left,anchor=west] (dout)   {$\hat D_t$};
\node[right=2mm of dunc,align=left,anchor=west]   (uout)   {$\log\sigma_t^2$};

\draw[gatearrow] (dunc.south) ..
  controls +(0,-10mm) and +(20mm,-15mm) ..
  node[below right]{write mask $w_t$} (vox.east);

\end{tikzpicture}%
}
\caption{ConvGRU-based instantiation of the persistent-memory framework used for all main results.
Solid arrows: forward pass; dashed red path: uncertainty-to-memory write gating (uncertainty variant only).
This diagram reflects our student-scale backbone, not the diffusion/DiT generator in the reference
manuscript.}
\label{fig:architecture}
\end{figure}

\begin{table}[!htb]
\centering
\small
\begin{tabular}{p{0.22\textwidth}p{0.35\textwidth}p{0.35\textwidth}}
\toprule
Aspect & Reference diffusion-style backbone & Our ConvGRU backbone \\
\midrule
Generator type &
Stochastic generative model (diffusion / iterative denoising) &
Deterministic recurrent predictor (one forward pass) \\
Typical compute / scale &
High (long training, large models) &
Lower (student-scale budgets) \\
Faithfulness to paper generator & High & Simplified (not a drop-in replacement) \\
Role in this project &
Future-work direction & primary controlled experiments \\
\bottomrule
\end{tabular}
\caption{The ConvGRU backbone is a \emph{substitute} for tractable experiments, not a claim of superiority over diffusion.}
\label{tab:backbone-compare}
\end{table}

\subsection{Data pipeline}

We export MOVi-A clips into compressed \texttt{.npz} shards containing RGB, depth, poses, intrinsics, and
metadata. A manifest lists shard paths for training scripts. We sample overlapping context/target windows with
capped counts per split and a fixed \texttt{val\_ratio=0.2}, so all models see identical splits.

\subsection{3D memory construction}
\label{sec:method-memory}

Let $P \in \mathrm{SE}(3)$ denote the \textbf{camera-to-world} pose, stored in the codebase as a
$4\times4$ matrix \texttt{camera\_to\_world} (same convention as Kubric exports: translation plus rotation
from camera axes to world axes).
Let $\tilde{\mathbf{u}}=[u,\,v,\,1]^\top$.
A camera-frame 3D point obtained by back-projecting $(u,v)$ with intrinsics $K$ and depth $D_t(u,v)$ is
$\mathbf{x}_c = D_t(u,v)\,K^{-1}\tilde{\mathbf{u}}$ in the usual pinhole \emph{z-depth} model.
MOVi/Kubric depth is \emph{radial} along a viewing ray; the implementation equivalently forms
$\mathbf{x}_c$ by scaling a unit ray from $K$ (\texttt{depth\_to\_camera\_points}) before applying $P$.
The corresponding world point is
\begin{equation}
\tilde{\mathbf{X}}_w = P\,\tilde{\mathbf{X}}_c,
\qquad
\tilde{\mathbf{X}}_c =
\begin{bmatrix}
\mathbf{x}_c \\[2pt] 1
\end{bmatrix}.
\end{equation}
For the textbook identity $\mathbf{x}_c = D_t(u,v)\,K^{-1}\tilde{\mathbf{u}}$, this is equivalently
\begin{equation}
\tilde{\mathbf{X}}_w = P
\begin{bmatrix}
D_t(u,v)\,K^{-1} [u,\, v,\, 1]^\top \\[2pt]
1
\end{bmatrix}.
\end{equation}
A \textbf{world-to-camera} pose would appear as $P^{-1}$ on world points; we do \emph{not} define $P$ that
way here, so there is no $P^{-1}$ in the unprojection---only $P$ mapping camera coordinates forward into
the world frame.

We voxelise these world points on a fixed grid (resolution $48{\times}40{\times}48$) by splatting RGB from
context frames into $\mathcal{M}$.

\subsection{Memory rendering and reading}
\label{sec:method-render}

For each target \textbf{camera-to-world} pose $P_t$, we render $\mathcal{M}$ into a target-aligned memory tensor $R_t$, a coverage
mask $m^{\mathcal{M}}_t$ (where the render is supported), and a rendered depth. A memory encoder $E_m$
maps $R_t$ to features compatible with the image encoder for fusion.

\subsection{ConvGRU prediction backbone}

A ConvGRU~\cite{convgru} maintains a spatial hidden state and is updated once per predicted timestep. We
encode each context frame, unroll over context to obtain $h_{T_c}$, then for each target time fuse image
features, memory features, and pose embeddings. Warm-starting (memory from no-memory; uncertainty from
memory) keeps optimisation stable.

\subsection{Baselines}

\paragraph{No-memory ConvGRU.}
Predicts future RGB from encoded context and pose only. Training uses RGB L1 plus dynamic-region L1.

\paragraph{Memory-conditioned ConvGRU.}
The model adds $E_m(R_t)$ to fusion and depth decoding. Training adds memory-covered L1 so optimisation
prefers predictions that agree with the memory render where it is defined.

\subsection{Extension: uncertainty-gated memory writes}
\label{sec:method-uncertainty}

Let $h_t$ denote features at target time $t$. The RGB head implements a prediction map; we write abstractly
\begin{equation}
\hat I_t = f_\theta\bigl(h_t, \text{pose}, \text{memory}\bigr).
\end{equation}
The uncertainty head predicts per-pixel log-variance / scores
\begin{equation}
s_t(u,v) = \bigl(U(h_t)\bigr)(u,v),
\end{equation}
with $\log\sigma_t^2 = \mathrm{clip}(U(h_t))$ for numerical stability. Training adds Gaussian negative
log-likelihood (Section~\ref{sec:method-loss}) with
$\bar{\delta}_t^2 = \frac{1}{3}\sum_c (\hat I_t^{(c)} - I_t^{(c)})^2$.

Confidence and a binary write mask are
\begin{equation}
c_t = \frac{e^{-\log\sigma_t^2}}{1 + e^{-\log\sigma_t^2}},
\qquad
\tilde c_t = c_t^{\gamma},
\qquad
w_t = \mathbb{1}\bigl[\tilde c_t \ge \tau\bigr],
\end{equation}
with $\gamma=4.0$ and $\tau=0.99$. \textbf{Observed context} frames are written into memory with full trust;
\textbf{generated} RGB from rollout is written only where $w_t{=}1$, so unreliable predictions are less
likely to corrupt $\mathcal{M}$. Abstractly, letting $v$ index voxels projected to pixel $(u,v)$,
\begin{equation}
\mathcal{M}_{t+1}(v) =
\begin{cases}
\mathrm{update}\bigl(\mathcal{M}_t(v), \text{predicted colour}\bigr) & \text{if } w_t(u,v)=1 \\
\mathcal{M}_t(v) & \text{otherwise.}
\end{cases}
\end{equation}

\subsection{Encoder details, recurrence, and loss functions}
\label{sec:method-loss}

\paragraph{Encoder and ConvGRU.}
Each frame is encoded by $E:\mathbb{R}^{3\times H\times W}\rightarrow\mathbb{R}^{C\times h\times w}$ with
$C{=}128$. ConvGRU gates follow~\cite{convgru}; we unroll over context to $h_{T_c}$.

\paragraph{Relative pose features.}
For anchor pose $P_a$ (last context pose) and target $P_t$,
$\phi(P_a,P_t) = \mathrm{vec}\bigl( (P_a^{-1} P_t)_{[:3,:4]} \bigr)\in\mathbb{R}^{12}$, passed through an
MLP and broadcast spatially.

\paragraph{Target-time recurrence (memory variants).}
\begin{align}
g_t &= \mathrm{Conv}_{1\times1}\bigl(
  [\,E(\hat I_{t-1}),\, E_m(R_t),\, \phi(P_a,P_t)\,]\bigr),
\quad
h_t = \mathrm{ConvGRU}(g_t, h_{t-1}), \\
\hat I_t &= \mathrm{clip}_{[0,1]}\bigl(
\hat I_{t-1} + \alpha\,\tanh\bigl(D_{\text{rgb}}(h_t)\bigr)\bigr),
\qquad
\hat D_t = \mathrm{ReLU}\bigl(D_d(h_t)\bigr),
\end{align}
with $\alpha=0.25$, initialising $\hat I_{T_c}=I_{T_c}$. The no-memory baseline omits $E_m(R_t)$ and uses a
simpler fusion of $E(\hat I_{t-1})$ with pose.

\paragraph{Masks and composite loss.}
Dynamic mask threshold $\theta=0.03$. Losses:
\begin{align}
\mathcal{L}_{\text{rgb}} &= \tfrac{1}{T_p HW}\sum_{t,c,u,v}
|I_t - \hat I_t|, \\
\mathcal{L}_{\text{dyn}} &= \mathrm{MaskedL1}(\hat I, I; m^{\text{dyn}}), \\
\mathcal{L}_{\text{depth}} &= \mathrm{MaskedL1}(\hat D, D; \mathbb{1}[D>0]), \\
\mathcal{L}_{\mathcal{M}} &= \mathrm{MaskedL1}(\hat I, I; m^{\mathcal{M}}), \\
\mathcal{L}_{\text{NLL}} &=
\tfrac{1}{T_p HW}\sum_{t,u,v}
\Bigl[\tfrac{1}{2}e^{-\log\sigma_t^2}\,\bar{\delta}_t^2
+ \tfrac{1}{2}\log\sigma_t^2\Bigr], \\
\mathcal{L}_{\text{total}} &=
\mathcal{L}_{\text{rgb}}
+ \lambda_{\text{dyn}}\mathcal{L}_{\text{dyn}}
+ \lambda_{\text{depth}}\mathcal{L}_{\text{depth}}
+ \lambda_{\mathcal{M}}\mathcal{L}_{\mathcal{M}}
+ \lambda_{\text{NLL}}\mathcal{L}_{\text{NLL}}.
\end{align}
Weights: $\lambda_{\text{dyn}}=3$ always; $\lambda_{\text{depth}}=0.1$, $\lambda_{\mathcal{M}}=1$ for memory
variants; $\lambda_{\text{NLL}}=0.25$ for the uncertainty variant. No-memory keeps only
$\mathcal{L}_{\text{rgb}}$ and $\mathcal{L}_{\text{dyn}}$.

\section{Experimental Setup}
\label{sec:setup}

\paragraph{Dataset and motivation.}
MOVi-A (Kubric~\cite{kubric}) offers RGB-D video with intrinsics and poses in controlled synthetic scenes,
which supports geometric debugging of unprojection and voxel memory. We use $200$ clips at source resolution
$128\times128$, split into $160$ train / $40$ validation clips with a fixed seed. Up to $16$ train and $8$
validation windows per clip yield $2{,}560$ train windows and $320$ validation windows. Each window uses
$T_c=T_p=4$; models consume RGB at $64\times64$.

\paragraph{Models compared (main results).}
Three ConvGRU variants: (1)~no-memory; (2)~memory-conditioned; (3)~memory with uncertainty-gated writes.
We do \textbf{not} include full diffusion/DiT reproduction in the main quantitative claims.

\paragraph{Architecture.}
Hidden channels $128$; voxel grid $(48,40,48)$, stride $1$, splat radius $1$ for memory variants.

\paragraph{Training.}
No-memory and memory: $50$ epochs, batch size $16$, Adam lr $10^{-3}$. Uncertainty: up to $40$ epochs,
warm-started from the memory checkpoint. Checkpoints are selected by best validation L1.

\paragraph{Loss hyperparameters.}
Dynamic weight $3.0$; depth weight $0.1$; memory-covered weight $1.0$; memory render loss weight $0.0$;
uncertainty NLL weight $0.25$; $\gamma=4.0$, $\tau=0.99$.

\paragraph{Hardware.}
Training and evaluation used single-GPU workstations and cluster GPUs (e.g., L40S-class). Memory
conditioning dominates runtime versus the no-memory baseline.

\section{Evaluation Protocol}
\label{sec:eval}

We load each model's best checkpoint on the same $320$ validation windows.

\paragraph{Metrics (why they matter).}
\begin{itemize}[leftmargin=1.3em,itemsep=2pt]
  \item \textbf{Val L1} ($\downarrow$): overall RGB reconstruction error---global prediction quality.
  \item \textbf{Dynamic L1} ($\downarrow$): error on moving/changing pixels---stress-tests temporal change.
  \item \textbf{Memory-covered L1} ($\downarrow$): error where oracle memory render is non-empty---whether
        memory-supported regions improve when memory is in play.
  \item \textbf{PSNR} ($\uparrow$): logarithmic quality summary correlated with perceptual sharpness (within
        limits at $64\times64$).
  \item \textbf{Uncertainty--error correlation} (uncertainty model): tests whether $s_t$ tracks actual error,
        supporting gating.
  \item \textbf{Write coverage}: mean fraction of pixels passing the gate under $\tau,\gamma$---how
        selective writes are at runtime.
\end{itemize}

\paragraph{Diagnostic slices.}
We aggregate windows with high motion, occlusion-recovery semantics, high memory coverage, and depth-edge
density (top fractions within validation). On our MOVi-A subset the camera is effectively static, so a
\textbf{high camera motion} slice is empty and omitted---this limits conclusions about viewpoint-heavy
generalisation.

\section{Results}
\label{sec:results}

\subsection{Does persistent memory help?}

Table~\ref{tab:main-results} compares the no-memory ConvGRU to the memory-conditioned ConvGRU.
Memory conditioning uniformly improves validation L1, dynamic L1, memory-covered L1, and PSNR---consistent
with the hypothesis that explicit 3D memory helps future RGB prediction even when the generator is a compact
recurrent model rather than a diffusion stack.

\begin{table}[!htb]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
Model & Val L1 $\downarrow$ & Dyn L1 $\downarrow$ & Mem-cov L1 $\downarrow$ & PSNR (dB) $\uparrow$ & Unc-corr & Write-cov \\
\midrule
ConvGRU no-memory & $0.0214$ & $0.1023$ & $0.0653$ & $25.57$ & --- & --- \\
ConvGRU memory & $0.0188$ & $\mathbf{0.0890}$ & $0.0577$ & $26.68$ & --- & $0.000$ \\
ConvGRU memory + uncertainty & $\mathbf{0.0185}$ & $0.0899$ & $\mathbf{0.0443}$ & $\mathbf{26.71}$ & $0.150$ & $0.973$ \\
\bottomrule
\end{tabular}%
}
\caption{Unified validation metrics (320 windows). Metrics read from the unified evaluation JSON file under
\texttt{outputs/eval\_final/} (checkpoints trained under \texttt{outputs/local\_movia200\_64\_convgru\_*\_v1}).}
\label{tab:main-results}
\end{table}

\subsection{Does uncertainty-gated writing help?}

Comparing the memory model to memory+uncertainty, the largest targeted gain is on \textbf{memory-covered L1}
($0.0577\!\rightarrow\!0.0443$; $-23.2\%$), the metric most sensitive to whether writes and reads maintain
faithful geometry in memory-supported regions. Overall L1 and PSNR improve slightly; dynamic L1 moves by
$0.0009$ (small regression, likely within optimisation noise at $64\times64$).
Uncertainty correlates positively with error ($r{=}0.150$), indicating signal usable for gating despite weak
absolute correlation. Write coverage $0.973$ shows the threshold leaves most pixels writable while still
allowing sparse suppressions where confidence drops---the mask pattern in Figure~\ref{fig:uncertainty_strips}
matters more than the scalar mean alone.

\subsection{Slice-level evaluation}

Table~\ref{tab:slices} reports L1 on diagnostic slices. The ranking
uncertainty $\le$ memory $<$ no-memory holds on each listed slice, with the uncertainty variant best on
all non-trivial slices.

\begin{table}[!htb]
\centering
\small
\begin{tabular}{lccc}
\toprule
Slice (count) & No-memory & Memory & Memory + uncertainty \\
\midrule
all (320) & $0.0214$ & $0.0188$ & $\mathbf{0.0185}$ \\
high\_motion (80) & $0.0295$ & $0.0263$ & $\mathbf{0.0260}$ \\
occlusion\_recovery (37) & $0.0232$ & $0.0205$ & $\mathbf{0.0203}$ \\
high\_memory\_coverage (80) & $0.0287$ & $0.0259$ & $\mathbf{0.0256}$ \\
depth\_edge\_heavy (80) & $0.0276$ & $0.0246$ & $\mathbf{0.0243}$ \\
\bottomrule
\end{tabular}
\caption{Validation L1 by slice. \emph{high\_camera\_motion} is omitted (empty) because cameras are
effectively static on this subset.}
\label{tab:slices}
\end{table}

\subsection{Qualitative results}

Although predictions remain blurry at $64\times64$, memory-conditioned and uncertainty-gated variants align
better with the quantitative trends: lower error in dynamic and memory-covered regions.
Figures~\ref{fig:panel_normal}--\ref{fig:panel_high_memory} summarise representative windows; they are
intended as interpretability aids, not photorealistic generation claims.

\begin{figure}[!htb]
\centering
\includegraphics[width=0.95\linewidth]{panel_normal_case.png}
\caption{Representative (median) validation window. Although predictions remain blurry at $64\times64$, the
memory and uncertainty variants improve quantitative error in memory-supported and dynamic regions relative to
no-memory. Uncertainty is elevated near object boundaries and disocclusions---regions where generated
predictions are least reliable.}
\label{fig:panel_normal}
\end{figure}

\begin{figure}[!htb]
\centering
\includegraphics[width=0.95\linewidth]{panel_high_motion.png}
\caption{High-motion slice. Same caveats as Figure~\ref{fig:panel_normal}; trends mirror lower dynamic L1 in
Table~\ref{tab:main-results}.}
\label{fig:panel_high_motion}
\end{figure}

\begin{figure}[!htb]
\centering
\includegraphics[width=0.95\linewidth]{panel_occlusion_recovery.png}
\caption{Occlusion-recovery slice. Blur remains visible; nonetheless ordering across models follows Table~\ref{tab:slices}.}
\label{fig:panel_occlusion}
\end{figure}

\begin{figure}[!htb]
\centering
\includegraphics[width=0.95\linewidth]{panel_high_memory_coverage.png}
\caption{High memory-coverage slice, where memory renders carry the most signal. Uncertainty maps highlight
riskier write regions.}
\label{fig:panel_high_memory}
\end{figure}

\subsection{Uncertainty maps and write masks}

Figure~\ref{fig:uncertainty_strips} isolates per-pixel confidence and the binary write mask for one
validation window. Brighter confidence means the model self-reports higher certainty (lower predicted
variance). The write mask is white where $\tilde c_t\ge\tau$ and black where updates to $\mathcal{M}$ from
\emph{generated} pixels are suppressed. Context observations are still written with full trust; the mask
primarily regulates contamination from uncertain hallucinated content during rollout.

\begin{figure}[!htb]
\centering
\includegraphics[width=0.46\linewidth]{uncertainty_strip.png}
\hfill
\includegraphics[width=0.46\linewidth]{uncertainty_write_mask.png}
\caption{Left: predicted confidence (higher intensity $=$ higher confidence). Right: write gate
(white $=$ allow generated-frame memory update, black $=$ hold previous voxel). Together with
Table~\ref{tab:main-results}, this illustrates how gating concentrates around ambiguous regions while keeping
overall write coverage high.}
\label{fig:uncertainty_strips}
\end{figure}

\subsection{Diffusion backbone not fully reproduced}

The reference manuscript centres a diffusion-style generative predictor. Training a comparable conditional
video diffusion/DiT model to convergence would require substantially more GPU time and tuning than budgeted
here. Repository scripts document a lightweight diffusion branch for \emph{exploratory} runs; we do
\emph{not} report diffusion metrics as main results, and we do not frame diffusion as a ``failed'' baseline---it
remains principled future work with adequate compute.

\section{Discussion}

\paragraph{Framework replication.}
We reproduced the persistent memory \emph{loop}---write, render/read, predict, update---at a scale compatible
with a graduate course project, without asserting a full match to every module in the reference manuscript.

\paragraph{Memory helps.}
Memory-conditioned ConvGRU improves over no-memory on standard and memory-centric metrics, supporting the
claim that geometric memory matters for student-scale video forecasting even when the generator is not
diffusion-sized.

\paragraph{Uncertainty and memory reliability.}
Uncertainty-gated writing primarily improves memory-covered L1, which is exactly where memory should pay off.
In persistent world models, prediction errors are not isolated: once written into memory, they can bias future
reads. Selective writes mitigate this failure mode.

\paragraph{Calibration and thresholds.}
$\gamma$ and $\tau$ matter because sigmoid confidence can saturate; the operating point balances coverage and
selectivity (Table~\ref{tab:main-results}).

\paragraph{Compute-feasible backbone.}
ConvGRU was selected as the primary backbone because it enabled controlled experiments under available compute,
not because it is generically superior to diffusion for video generation.

\paragraph{Diffusion as future work.}
A stronger diffusion/DiT predictor paired with the \emph{same} memory loop is the natural next step when GPU
budget allows.

\section{Limitations}

\begin{itemize}[leftmargin=1.3em,itemsep=3pt]
  \item \textbf{Not full architecture replication:} no full diffusion/DiT generator at reference scale, no
        DINO-style semantic feature memory, no large-scale embodied training as in the manuscript's intended
        regime.
  \item \textbf{Compute:} diffusion-scale training was out of scope for final claims.
  \item \textbf{Dataset:} MOVi-A is synthetic and controlled; transfer to Habitat, HM3D, or Real-World Robotics
        is not established here.
  \item \textbf{Resolution:} $64\times64$ caps detail and qualitative fidelity.
  \item \textbf{Uncertainty:} informative correlation, not guaranteed calibration for arbitrary deployments.
  \item \textbf{Memory representation:} dense voxels do not encode explicit object semantics.
  \item \textbf{Camera motion:} static cameras in our subset mean weak stress tests for viewpoint change.
\end{itemize}

\section{Conclusion and Future Work}

We presented a student-scale \emph{framework} replication of persistent 3D world modeling with an
uncertainty-gated memory update extension. We do \emph{not} claim to reproduce the full diffusion-based
generator from the reference manuscript. Empirically, persistent memory improves a ConvGRU forecaster over a
no-memory baseline, and uncertainty-gated writes improve memory-covered prediction---our main proxy for
memory reliability.

Future work includes: (i)~scaling the generative backbone toward diffusion/DiT while retaining the same
memory interface; (ii)~richer MOVi splits or MOVi-C/D-style diversity; (iii)~evaluation on embodied RGB-D
benchmarks with real camera motion; (iv)~object-level or semantic uncertainty and feature memory; (v)~calibrating
confidence maps for principled threshold selection.

\section{Reproducibility}
\label{sec:repro}

\sloppy
\begin{itemize}[leftmargin=1.3em,itemsep=3pt]
  \item Environment and data export: see \texttt{README.md}.
  \item \textbf{Main results pipeline:} \path{scripts/train_convgru_all.sh}; evaluation via
        \path{scripts/eval_all.py} produces \path{outputs/eval_final/evaluation.json} (numbers in
        Table~\ref{tab:main-results}).
  \item \textbf{Local checkpoint directories} used to build Table~\ref{tab:main-results}:
        \path{outputs/local_movia200_64_convgru_nomemory_v1};
        \path{outputs/local_movia200_64_convgru_memory_v1};
        \path{outputs/local_movia200_64_convgru_uncertainty_v1}.
        Table metrics correspond to evaluating these \textbf{local} checkpoints (the archived
        \path{outputs/eval_final/evaluation.json}); \path{outputs/scc_movia200_64_convgru_*} paths name
        separate cluster training runs and are not the source of the quoted numbers unless evaluation is
        re-run with those directories as the checkpoint roots.
  \item \textbf{Optional cluster workflow:} on shared clusters, the same stages can be orchestrated with SGE
        scripts
        \path{scripts/scc/qsub_train_convgru_all.sh},
        \path{scripts/scc/qsub_train_diffusion_all.sh},
        \path{scripts/scc/qsub_eval_compare.sh}, and
        \path{scripts/scc/qsub_make_visuals.sh}
        when present in a checkout. The ConvGRU script reproduces the main quantitative
        pipeline; the diffusion script is optional exploratory / future-work tooling.
  \item \textbf{Typical cluster output layout (if used):}
        \path{outputs/scc_movia200_64_convgru_nomemory_v1};
        \path{outputs/scc_movia200_64_convgru_memory_v1};
        \path{outputs/scc_movia200_64_convgru_uncertainty_v1};
        \path{outputs/scc_final_eval_movia200_64_v1};
        \path{outputs/scc_final_model_comparison_movia200_64_v1}.
  \item Panels: \path{scripts/make_visual_comparison_panel.py}; figures under \path{outputs/report_figures/}.
  \item \textbf{Exploratory diffusion:} \path{scripts/train_diffusion_all.sh} (not used for main claims in
        this paper).
\end{itemize}
\fussy

\begin{thebibliography}{9}

\bibitem{persistent3dworld}
\emph{Learning 3D Persistent Embodied World Models.}
Course reference manuscript, 2025.

\bibitem{kubric}
K.~Greff, F.~Belletti, L.~Beyer, et al.
``Kubric: A Scalable Dataset Generator.''
\emph{Proceedings of the IEEE/CVF Conference on Computer Vision and
Pattern Recognition (CVPR)}, 2022.

\bibitem{convlstm}
X.~Shi, Z.~Chen, H.~Wang, D.-Y.~Yeung, W.-K.~Wong, and W.-c.~Woo.
``Convolutional LSTM Network: A Machine Learning Approach for
Precipitation Nowcasting.''
\emph{Advances in Neural Information Processing Systems (NeurIPS)},
2015.

\bibitem{convgru}
N.~Ballas, L.~Yao, C.~Pal, and A.~Courville.
``Delving Deeper into Convolutional Networks for Learning Video
Representations.''
\emph{International Conference on Learning Representations (ICLR)},
2016.

\bibitem{videodiffusion}
J.~Ho, T.~Salimans, A.~Gritsenko, W.~Chan, M.~Norouzi, and D.~Fleet.
``Video Diffusion Models.''
\emph{Advances in Neural Information Processing Systems (NeurIPS)},
2022.

\bibitem{kendallgal2017}
A.~Kendall and Y.~Gal.
``What Uncertainties Do We Need in Bayesian Deep Learning for
Computer Vision?''
\emph{Advances in Neural Information Processing Systems (NeurIPS)},
2017.

\end{thebibliography}

\end{document}
